# 风火轮小组 — 工作指引

## 关键规则

1. **不要扫描历史对话** — 所有历史 plan 文件都不存在，不要读 jsonl 日志
2. **不要用 python3 -c 跑任何命令** — 直接写文件
3. **只用 Write 工具创建文件**，不要跑 bash/python 浏览目录
4. **脚本都放 template/gates/ 目录**
5. **写完后 git push**
